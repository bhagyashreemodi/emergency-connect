/**
 * This class is a database implementation, the dependency of DAO.
 * DAO gets its dependency via dependency injection: `DAO.db = new InMemoryDBImpl()`
 */

// TODO: This class MUST implement all APIs in `MongoDBImpl`, since they ought to implement a same interface
export default class InMemoryDBImpl {

}