<div class="container-fluid container-main">
    <%- include('tab-header', {
        title: 'Chat With User',
        backButton: true,
        searchButton: true
    }) %>
    <!-- 
    <div class="py-2 px-3 d-flex justify-content-between align-items-center" style="height: 48px;">
        <div class="tab-header justify-content-start">
            <div class="pr-2">
                <span class="fa fa-arrow-left" id="closePrivateChatBtn"> </span>
            </div>
            <div>
                <span class="username" id="privateChatTarget">Chat with Username</span>
            </div>
        </div>
        <button type="button" class="btn btn-sm btn-secondary ml-2" id="searchButton">
            <i class="fas fa-search"></i>
        </button>
    </div>

    <div class="divider"></div>
    -->
    
    <ul class="chat-message-list" id="messages">
        <!-- Messages will be dynamically inserted here -->
    </ul>

    <%- include('chat-input-form', { formId: 'postMessageForm', inputId: 'messageInput', placeholder: 'Type your message...' }) %>

</div>
